export function Background() {
  return (
    <div
      className="
        absolute inset-0 -z-50 bg-[radial-gradient(farthest-side,var(--cyan2),transparent)]
        md:max-h-screen
      "
    />
  );
}
